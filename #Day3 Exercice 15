function fusionner(tab1, tab2) {
  const resultat = [];
  
  while (tab1.length && tab2.length) {
   
    if (tab1[0] < tab2[0]) resultat.push(tab1.shift());
    else resultat.push(tab2.shift());
  }


  return [...resultat, ...tab1, ...tab2];
}


console.log(fusionner([1, 3, 5], [2, 4, 6])); 
