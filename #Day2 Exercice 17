let totalItems = 45;
const itemsPerPage = 10;
let page = 1;

while (totalItems > 0) {
  let currentItems = Math.min(totalItems, itemsPerPage);
  console.log(`Scraping page ${page} (${currentItems} items)`);
  
  totalItems -= currentItems;
  page++;
}
