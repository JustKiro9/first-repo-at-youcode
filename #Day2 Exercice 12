let nb = 29;
let estPremier = true;

for (let i = 2; i < nb; i++) {
  if (nb % i === 0) {
    estPremier = false;
    break;
  }
}

if (estPremier) {
  console.log(`${nb} est un nombre premier`);
} else {
  console.log(`${nb} n'est pas un nombre premier`);
}
